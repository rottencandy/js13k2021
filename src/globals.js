/** Canvas element */
export let CANVAS = 0;
export const
  GAME_WIDTH = 800,
  GAME_HEIGHT = 600,
  WIDTH = 'width',
  HEIGHT = 'height',
  STYLE = 'style',
  /** Set Canvas element */
  setCanvas = (ele) => CANVAS = ele;

// vim: fdm=marker:et:sw=2:
