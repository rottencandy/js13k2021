/** @type {HTMLCanvasElement} */
export let CANVAS;
export const
  GAME_WIDTH = 800,
  GAME_HEIGHT = 600,
  FOV = 1.047,
  ZNEAR = 1,
  ZFAR = 2000,
  WIDTH = 'width',
  HEIGHT = 'height',
  STYLE = 'style',
  PLATFORM_SIZE = 50,
  /** Set Canvas element */
  setCanvas = (ele) => CANVAS = ele;

// vim: fdm=marker:et:sw=2:
