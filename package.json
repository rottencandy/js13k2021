{
  "name": "js13k2021",
  "version": "0.0.1",
  "main": "index.js",
  "author": "Mohammed Saud <md.saud020@gmail.com>",
  "license": "MIT",
  "scripts": {
    "prebuild": "mkdir -p app && cp assets/* app",
    "build": "node scripts/build.js",
    "predev": "mkdir -p app && cp assets/* app",
    "dev": "node scripts/dev.js",
    "uglify": "uglifyjs --compress --mangle --mangle-props -o app/main.js -- app/main.js",
    "bundle": "./bundle.sh"
  },
  "dependencies": {
    "esbuild": "^0.12.19",
    "esbuild-plugin-glslx": "^0.0.2",
    "uglify-js": "^3.14.1"
  }
}
